<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juandaqugo.elsantuario">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PerfilActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".DemoActivity" />
        <!--
            <activity
            android:name=".HotelesActivity"
            android:label="@string/title_activity_hoteles"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        -->
        <activity
            android:name=".fragmentsActivity"
            android:label="@string/title_activity_fragments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListaActivity" />
        <activity
            android:name=".HotelActivity"
            android:label="@string/title_activity_hotel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SitiosActivity"
            android:label="@string/title_activity_sitios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DrawerBaresActivity"
            android:label="@string/title_activity_drawer_bares"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DrawerHotelesActivity"
            android:label="@string/title_activity_drawer_hoteles"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DrawerMainActivity"
            android:label="@string/title_activity_drawer_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
            <activity
            android:name=".DrawerRestaurantesActivity"
            android:label="@string/title_activity_drawer_restaurantes"
            android:theme="@style/AppTheme.NoActionBar" />
        -->
        <activity
            android:name=".DrawerPerfilActivity"
            android:label="@string/title_activity_drawer_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DrawerSitiosActivity"
            android:label="@string/title_activity_drawer_sitios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Restaurante1Activity" />
        <activity android:name=".Restaurante2Activity" />
        <activity android:name=".Restaurante3Activity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"/>
    </application>

</manifest>